{
  "name": "personal-finance-tracker",
  "version": "1.0.0",
  "description": "Personal finance tracking app with Android and web components",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "cd web && npm run build",
    "install-all": "npm install && cd web && npm install",
    "setup": "node setup-local-data.js",
    "setup-full": "npm run setup && npm run install-all"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["finance", "tracking", "android", "web"],
  "author": "Your Name",
  "license": "MIT"
}
